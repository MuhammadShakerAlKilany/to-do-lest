import { StrikeToDoPipe } from './strike-to-do.pipe';

describe('StrikeToDoPipe', () => {
  it('create an instance', () => {
    const pipe = new StrikeToDoPipe();
    expect(pipe).toBeTruthy();
  });
});
